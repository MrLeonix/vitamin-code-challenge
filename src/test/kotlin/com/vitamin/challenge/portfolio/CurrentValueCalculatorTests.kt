package com.vitamin.challenge.portfolio

import com.vitamin.challenge.*
import com.vitamin.challenge.services.FmpService
import com.vitamin.challenge.services.PortfolioService
import io.mockk.coEvery
import io.mockk.coJustRun
import io.mockk.every
import io.mockk.mockk
import org.junit.jupiter.api.Assertions
import org.junit.jupiter.api.BeforeAll
import org.junit.jupiter.api.Test

class CurrentValueCalculatorTests {
    @Test
    fun `calculates monthly-based`(){
        //TODO: Given, When, Then
    }

    @Test
    fun `ensure shares and values are correctly calculated`(){
        //TODO: Given, When, Then
    }

    @Test
    fun `all stocks are fetched from FMP API`(){
        //TODO: Given, When, Then
    }
}